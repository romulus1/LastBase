<?php
    require_once(__DIR__ . "/view/header.php"); //checks the view folder for  the php file and uses it as a post
    require_once(__DIR__ . "/view/form.php"); //checks the view folder form.php for the php file and uses it as a post
    require_once(__DIR__ . "/view/footer.php"); //checks the view footer.php folder for the php file and uses it as a post

